[package]
name = "skelly-session"
description = "Session timeline, non-destructive rewind (snapshot store), and per-repo git diff."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
thiserror.workspace = true

[dev-dependencies]
# Integration tests drive a real `git` against a throwaway repo in a temp dir.
tempfile = "3"
